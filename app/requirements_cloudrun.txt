# ElectionWatch Cloud Run Requirements
# Minimal dependencies for production deployment

# Core FastAPI service
fastapi>=0.100.0
uvicorn[standard]>=0.22.0
pydantic>=2.4.0

# HTTP clients
requests>=2.28.0

# Optional Google Cloud (graceful fallback if not available)
google-cloud-aiplatform>=1.88.0

# Basic data processing
pandas>=1.5.0
numpy>=1.24.0 