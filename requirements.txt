# ElectionWatch Agent API - Cloud Run Requirements
# Minimal dependencies for running the FastAPI agent service

# Core FastAPI service (updated for ADK compatibility)
fastapi>=0.115.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
python-multipart>=0.0.6
python-dotenv>=1.0.0

# Google Cloud dependencies
google-cloud-core>=2.4.1
google-auth>=2.23.4

# Updated ADK and AI Platform versions to resolve conflict
google-adk==1.5.0; python_version>="3.8"
google-cloud-aiplatform[agent_engines]>=1.95.1; python_version>="3.8"

# Basic data processing (lightweight versions)
pandas>=2.1.4
numpy>=1.24.4

# HTTP client for API calls (updated for ADK compatibility)
aiohttp>=3.9.1
requests>=2.32.4 